export declare class DebitTransaction {
    SourceAccountNumber: string;
    Amount: number;
    Memo: string;
    DestinationBank: string;
    DestinationAccountNumber: string;
    constructor(SourceAccountNumber: string, Amount: number, Memo: string, DestinationBank: string, DestinationAccountNumber: string);
}
